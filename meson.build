project('sdl_ecsprac', 'cpp',
        default_options: 'default_library=static')

sdl2_dep = dependency('sdl2')
imgui_dep = dependency('imgui')

executable('main', 'code/sdl_ecsprac.cpp',
           dependencies: [imgui_dep, sdl2_dep])


